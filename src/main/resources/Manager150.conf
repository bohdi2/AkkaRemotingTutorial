
akka {
  loglevel = "WARNING"

  actor {
    provider = "akka.remote.RemoteActorRefProvider"
  }

  remote.netty.tcp.hostname = ${manager.hostname}
  remote.netty.tcp.port = ${manager.port}
}

remotePath = "akka.tcp://workerSystem@"${worker.hostname}":"${worker.port}"/user/chattyWorker"